import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'iraqToursGuideIonicApp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
